"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function verify(code) {
    code = code.replace(/\D/g, ''); // just in case
    if (code.length !== 10)
        return false;
    var digits = code.split('') // spliting digits
        .map(function (digit) { return +digit; }) // parsing digits into number type
        .reverse(); // and finally reversing the array
    var onesDigit = digits[0];
    var sum = -onesDigit;
    digits.map(function (digit, index) { return sum += digit * (index + 1); });
    sum %= 11;
    if (sum < 2) {
        if (sum === onesDigit)
            return true;
    }
    else {
        sum = 11 - sum;
        if (sum === onesDigit)
            return true;
    }
    return false;
}
exports.verify = verify;
